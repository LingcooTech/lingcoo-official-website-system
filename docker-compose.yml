name: lingcoo-official-website

services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-lingcoo_official}
      POSTGRES_USER: ${POSTGRES_USER:-lingcoo_official}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lingcoo_official_password}
    ports:
      - '127.0.0.1:5438:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}']
      interval: 5s
      timeout: 5s
      retries: 12
    restart: unless-stopped

volumes:
  postgres_data:
