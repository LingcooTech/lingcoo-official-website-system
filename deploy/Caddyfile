lingcoo.com {
	redir https://www.lingcoo.com{uri} permanent
}

www.lingcoo.com {
	encode zstd gzip

	header {
		-Server
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
	}

	reverse_proxy api:8090
}
